# When will the else part of try-except-else be executed?

The else part of a try-except-else block will be executed only when no exceptions are raised in the try block. In other words, the else
block runs if everything in the try block runs successfully without encountering any errors.

