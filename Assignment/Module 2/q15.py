# Write a Python program to count occurrences of a substring in a string.
"""
incomplete
"""
name=input('Enter a string:')
name=name.lower()
count=0
start=0

if start == name.find(start):
    if start ==-1:
        count+=1
        start+=len(name)

